🧠 1️⃣ Core Idea
Command =
    "encapsulate a request as an object"

👉 Instead of:
    "calling methods directly"
You do:
    "create an object that REPRESENTS the action"

🎯 2️⃣ Real-Life Analogy
Remote Control
Button → Command
TV → Receiver
You → Client

👉 Button doesn’t know HOW TV works
👉 It just calls:
execute()

❌ 3️⃣ Problem Before Command
Scenario: Smart Home
(code)
🔴 Problems
    ❌ if/else explosion
    ❌ tightly coupled
    ❌ no flexibility
    ❌ cannot queue / undo

✅ 4️⃣ Command Solution
(code)

🔥 What Just Happened?
Instead of:
    button → logic ❌
You now have:
    button → command → receiver ✅

🧠 6️⃣ Mental Model
Command =
    "wrap a method call inside an object"

🎯 7️⃣ Why This Is Powerful
✅ 1. Decoupling
Invoker doesn’t know:
    what is being executed

✅ 2. Queueing
Queue<Command> queue;
👉 execute later

✅ 3. Undo/Redo
interface Command {
    void execute();
    void undo();
}

✅ 4. Logging & replay
    Store commands → replay later

🔥 8️⃣ Real Backend Examples:
1️⃣ Job Queues
    Command → job
    Queue → executor

2️⃣ Spring @Async / Task Executors
In Spring Framework:
    Runnable / Callable = Command

3️⃣ Transaction logs
    Store operations → replay

4️⃣ CQRS systems
Commands represent user actions:
    CreateOrderCommand
    CancelOrderCommand

⚠️ 9️⃣ Command vs Strategy
Strategy
    Choose behavior
Command
    Represent ACTION

👉 Key difference:
    Strategy = "how to do"
    Command = "what to do"

⚠️ 🔟 Command vs Chain
Chain
    passes request along handlers
Command
    represents one action

🧠 11️⃣ When You Should Use It
When:
    you want to treat actions as objects

🔥 Final Understanding
Command =
    "turn a function call into an object"

